spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: ${rabbitmq.username}
    password: ${rabbitmq.password}
rabbit:
  event:
    queue: q.event-queue
management:
  endpoints:
    web:
      exposure:
        include: "*"